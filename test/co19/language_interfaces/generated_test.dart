// Generated by co19_adapter — do not edit manually.
// ignore_for_file: unused_import

import 'package:dart_eval/dart_eval.dart';
import 'package:test/test.dart';

import '../_infrastructure/expect_plugin.dart';

void main() {
  group('co19 Language/Interfaces', () {
    test('Combined_Member_Signature > combined_member_signature_A02_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Now we define combined member signatures. Let id be an
/// identifier, L a id, L, Ij , k library, I1, . . . , Ik a list of interfaces,
/// and M0 the set of all member signatures M0 from I1, . . . , Ik named id and
/// accessible to L. The combined member signature named id from I1, . . . , Ik
/// with respect to L is the member signature which is obtained as follows:
/// ...
/// M0 contains more than one member signature m1, . . . , mq.
/// ...
/// Case ⟨Getters⟩. Determine whether there exists a non-empty set N ⊆ 1..q such
/// that for each i ∈ N, the return type of mi is a subtype of the return type
/// of mj for each j ∈ 1..q. If no such set exists, the computation of the
/// combined member signature failed.
///
/// Otherwise, when a set N as specified above exists, let Nall be the greatest
/// set satisfying the requirement on N, and let Mall = {mi | i ∈ Nall}.
///
/// Let m_all be the result of applying NNBD_TOP_MERGE to the elements in Mall,
/// ordered according to the interface I1 .. Ik that each signature came from.
///
/// The combined member signature is then m_all
///
/// @description Checks that for the getters the combined member signature is
/// the result of applying `NNBD_TOP_MERGE` to the elements in Mall
/// @author sgrekhov22@gmail.com

// [co19_adapter] removed Utils import

class I1 {
  Object? get m1 => 1;
  dynamic get m2 => 1;
  num get m3 => 1;
  Object? get m4 => 1;
  Object get m5 => 1;
}

class I2 {
  void get m1 => 2;
  void get m2 => 2;
  int get m3 => 2;
  Object get m4 => 1;
  String get m5 => "1";
}

abstract class C implements I1, I2 {}

mixin M implements I1, I2 {}

void test(C c, M m) {
  c.m1.expectStaticType<Exactly<Object?>>();
  c.m2.expectStaticType<Exactly<Object?>>();
  c.m3.expectStaticType<Exactly<int>>();
  c.m4.expectStaticType<Exactly<Object>>();
  c.m5.expectStaticType<Exactly<String>>();

  m.m1.expectStaticType<Exactly<Object?>>();
  m.m2.expectStaticType<Exactly<Object?>>();
  m.m3.expectStaticType<Exactly<int>>();
  m.m4.expectStaticType<Exactly<Object>>();
  m.m5.expectStaticType<Exactly<String>>();
}

main() {
  print(C);
  print(M);
  print(test);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Combined_Member_Signature > combined_member_signature_A03_t05', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Now we define combined member signatures. Let id be an
/// identifier, L a id, L, Ij , k library, I1, . . . , Ik a list of interfaces,
/// and M0 the set of all member signatures M0 from I1, . . . , Ik named id and
/// accessible to L. The combined member signature named id from I1, . . . , Ik
/// with respect to L is the member signature which is obtained as follows:
/// ...
/// M0 contains more than one member signature m1, . . . , mq.
/// ...
/// Case ⟨Methods and setters⟩. Determine whether there exists a non-empty set
/// N ⊆ 1..q such that for each i ∈ N, the return type of mi is a subtype of
/// the function type of mj for each j ∈ 1..q. If no such set exists, the
/// computation of the combined member signature failed.
///
/// Otherwise, when a set N as specified above exists, let Nall be the greatest
/// set satisfying the requirement on N, and let Mall = {mi | i ∈ Nall}.
///
/// Let mall be the result of applying NNBD_TOP_MERGE to the elements in Mall,
/// ordered according to the interface I1 .. Ik that each signature came from.
///
/// The combined member signature is then m′, which is obtained from mall by
/// adding the modifier covariant to each parameter p (if it is not already
/// present) when there exists a j ∈ 1..q such that the parameter corresponding
/// to p has the modifier covariant.
///
/// @description Checks that the combined member signature is the result of
/// applying `NNBD_TOP_MERGE` to the elements in `Mall`
/// @author sgrekhov22@gmail.com

// [co19_adapter] removed Utils import

class I1 {
  num m1() => 3.14;
  Object? m2() => 1;
  dynamic m3() => 1;
  Object? m4() => 1;
  Object m5() => 1;
}

class I2 {
  int m1() => 2;
  void m2() => 2;
  void m3() => 2;
  Object m4() => 1;
  String m5() => "1";
}

abstract class C implements I1, I2 {}

mixin M implements I1, I2 {}

test(C c, M m) {
  c.m1.expectStaticType<Exactly<int Function()>>();
  c.m2.expectStaticType<Exactly<Object? Function()>>();
  c.m3.expectStaticType<Exactly<Object? Function()>>();
  c.m4.expectStaticType<Exactly<Object Function()>>();
  c.m5.expectStaticType<Exactly<String Function()>>();

  m.m1.expectStaticType<Exactly<int Function()>>();
  m.m2.expectStaticType<Exactly<Object? Function()>>();
  m.m3.expectStaticType<Exactly<Object? Function()>>();
  m.m4.expectStaticType<Exactly<Object Function()>>();
  m.m5.expectStaticType<Exactly<String Function()>>();
}

main() {
  print(C);
  print(M);
  print(test);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Combined_Member_Signature > combined_member_signature_A03_t06', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Now we define combined member signatures. Let id be an
/// identifier, L a id, L, Ij , k library, I1, . . . , Ik a list of interfaces,
/// and M0 the set of all member signatures M0 from I1, . . . , Ik named id and
/// accessible to L. The combined member signature named id from I1, . . . , Ik
/// with respect to L is the member signature which is obtained as follows:
/// ...
/// M0 contains more than one member signature m1, . . . , mq.
/// ...
/// Case ⟨Methods and setters⟩. Determine whether there exists a non-empty set
/// N ⊆ 1..q such that for each i ∈ N, the return type of mi is a subtype of
/// the function type of mj for each j ∈ 1..q. If no such set exists, the
/// computation of the combined member signature failed.
///
/// Otherwise, when a set N as specified above exists, let Nall be the greatest
/// set satisfying the requirement on N, and let Mall = {mi | i ∈ Nall}.
///
/// Let mall be the result of applying NNBD_TOP_MERGE to the elements in Mall,
/// ordered according to the interface I1 .. Ik that each signature came from.
///
/// The combined member signature is then m′, which is obtained from mall by
/// adding the modifier covariant to each parameter p (if it is not already
/// present) when there exists a j ∈ 1..q such that the parameter corresponding
/// to p has the modifier covariant.
///
/// @description Checks that the combined member signature is the result of
/// applying `NNBD_TOP_MERGE` to the elements in `Mall`
/// @author sgrekhov22@gmail.com

// [co19_adapter] removed Utils import

class I1 {
  void m1(num n) {}
  void m2(Object? o) {}
  void m3(dynamic d) {}
  void m4(Object? o) {}
  void m5(Object o) {}
}

class I2 {
  void m1(int i) {}
  void m2(void v) {}
  void m3(void v) {}
  void m4(Object o) {}
  void m5(String s) {}
}

abstract class C implements I1, I2 {}

mixin M implements I1, I2 {}

test(C c, M m) {
  c.m1.expectStaticType<Exactly<void Function(num)>>();
  c.m2.expectStaticType<Exactly<void Function(Object?)>>();
  c.m3.expectStaticType<Exactly<void Function(Object?)>>();
  c.m4.expectStaticType<Exactly<void Function(Object?)>>();
  c.m5.expectStaticType<Exactly<void Function(Object)>>();

  m.m1.expectStaticType<Exactly<void Function(num)>>();
  m.m2.expectStaticType<Exactly<void Function(Object?)>>();
  m.m3.expectStaticType<Exactly<void Function(Object?)>>();
  m.m4.expectStaticType<Exactly<void Function(Object?)>>();
  m.m5.expectStaticType<Exactly<void Function(Object)>>();
}

main() {
  print(C);
  print(M);
  print(test);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Correct_Member_Overrides > correct_member_overrides_A03_t16', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let m and m′ be member signatures with the same name id. Then m
/// is a correct override of m′ iff the following criteria are all satisfied:
/// ...
/// • If m and m′ are both methods or both setters: Let F be the function type
/// of m except that the parameter type is the built-in class Object for each
/// parameter of m which is covariant-by-declaration. Let F′ be the function
/// type of m′. F must then be a subtype of F′.
///
/// @description Checks that it is not an error if `m` and `m′` are both methods
/// and function type `m` is a subtype of `m′. Test `extends` and `on` clauses
/// @author sgrekhov22@gmail.com

mixin class I {
  void m1(num v1) {}
  void m2(num v1, [num v2 = 0]) {}
  void m3(num v1, {num v2 = 0}) {}
  void m4(covariant int v1) {}
  void m5(num v1, [covariant int v2 = 0]) {}
  void m6(num v1, {covariant int v2 = 0}) {}
}

class C extends I {
  void m1(covariant int i) {}
  void m2(num v1, [covariant Object v2 = ""]) {}
  void m3(num v1, {covariant int v2 = 0}) {}
  void m4(num i) {}
  void m5(num v1, [Object v2 = ""]) {}
  void m6(num v1, {int v2 = 0}) {}
}

mixin M on I {
  void m1(covariant int i) {}
  void m2(num v1, [covariant Object v2 = ""]) {}
  void m3(num v1, {covariant int v2 = 0}) {}
  void m4(num i) {}
  void m5(num v1, [Object v2 = ""]) {}
  void m6(num v1, {int v2 = 0}) {}
}

main() {
  print(C);
  print(M);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > inheritance_t05', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2011, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// Otherwise, if the static types T1,...,Tk of the members m1,...,mk are not
/// identical, then there must be a member mx such that Tx <: Ti, 1 ≤ x ≤ k for
/// all i ∈ 1..k, or a static type warning occurs. The member that is inherited
/// is mx, if it exists; otherwise: let numberOf P ositionals(f) denote the
/// number of positional parameters of a function f, and let numberOf
/// RequiredParams(f) denote the number of required parameters of a function f.
/// Furthermore, let s denote the set of all named parameters of the m1,...,mk.
/// Then let
/// h = max(numberOf Positionals(mi)),
/// r = min(numberOf RequiredParams(mi)), i ∈ 1..k.
/// Then I has a method named n, with r required parameters of type dynamic,
/// h positional parameters of type dynamic, named parameters s of type dynamic
/// and return type dynamic.
/// @description Checks that there's no static warning produced when the
/// subinterface member being accessed is actually inherited. Members tested
/// include variables, methods, getters, setters and operators. There's no way
/// to check whether the resulting inherited method signature is actually as
/// described above, but we can check that it's inherited at all.
/// @author rodionov

import 'package:co19_expect/expect.dart';

class C {}
class D extends C {}

abstract class SI1 {
  int method(num? v, String? p, {int? o1, Pattern? o2});
  int method2(C? v, [D? o]);
  int? get gett0r;
  void set sett0r(int? v);
  C operator +(C v);
}

abstract class SI2 {
  void method(num? v, String? p, {int? o1, Pattern? o2});
  num method2(C? v, [D? o]);
  num? get gett0r;
  void set sett0r(num? v);
  C operator +(C v);
}

abstract class I implements SI1, SI2 {}

main() {
  I? i = null;
  dynamic d = null;

  i?.method(null, null, o1:null, o2:null);
  var v = i?.method2(null, null);
  num? n = i?.gett0r;
  i?.sett0r = null;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > inheritance_t06', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2011, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// Otherwise, if the static types T1,...,Tk of the members m1,...,mk are not
/// identical, then there must be a member mx such that Tx <: Ti, 1 ≤ x ≤ k for
/// all i ∈ 1..k, or a static type warning occurs. The member that is inherited
/// is mx, if it exists; otherwise: let numberOf P ositionals(f) denote the
/// number of positional parameters of a function f, and let numberOf
/// RequiredParams(f) denote the number of required parameters of a function f.
/// Furthermore, let s denote the set of all named parameters of the m1,...,mk.
/// Then let
/// h = max(numberOf Positionals(mi)),
/// r = min(numberOf RequiredParams(mi)), i ∈ 1..k.
/// Then I has a method named n, with r required parameters of type dynamic,
/// h positional parameters of type dynamic, named parameters s of type dynamic
/// and return type dynamic.
/// @description Checks that there's no static warning if class implements two
/// interfaces with same named methods and different number of required formal
/// parameters and defines method with minimum number of required parameters
/// and maximum number of positional parameters.
/// @author ngl@unipro.ru


abstract class SI1 {
  void foo(var v);
}

abstract class SI2 {
  void foo(var v, var vv);
}

class I implements SI1, SI2 {
  // It is expected that I inherits void foo(dynamic v1, [dynamic a])
  void foo(dynamic v, [dynamic a]) {}
}

main() {
  I i = new I();
  i.foo(1);
  i.foo(1,2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > inheritance_t08', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2011, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// Otherwise, if the static types T1,...,Tk of the members m1,...,mk are not
/// identical, then there must be a member mx such that Tx <: Ti, 1 ≤ x ≤ k for
/// all i ∈ 1..k, or a static type warning occurs. The member that is inherited
/// is mx, if it exists; otherwise: let numberOf P ositionals(f) denote the
/// number of positional parameters of a function f, and let numberOf
/// RequiredParams(f) denote the number of required parameters of a function f.
/// Furthermore, let s denote the set of all named parameters of the m1,...,mk.
/// Then let
/// h = max(numberOf Positionals(mi)),
/// r = min(numberOf RequiredParams(mi)), i ∈ 1..k.
/// Then I has a method named n, with r required parameters of type dynamic,
/// h positional parameters of type dynamic, named parameters s of type dynamic
/// and return type dynamic.
/// @description Checks that there's no static warning if non-abstract class
/// implements two interfaces with same named methods that have different named
/// formal parameters and defines method from its implicit interface. In this
/// case the implicit interface of non-abstract class has method with one
/// required parameter of type dynamic and a set of all named optional
/// parameters.
/// @author ngl@unipro.ru


abstract class SI1 {
  void foo(var v, {int foo, int bar});
}

abstract class SI2 {
  void foo(var v, {int foo, int b4r});
}

class I implements SI1, SI2 {
  void foo(dynamic v, {dynamic foo, dynamic b4r, dynamic bar}) {}
}

main() {
  I? i = null;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > inheritance_t13', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// Otherwise, if the static types T1,...,Tk of the members m1,...,mk are not
/// identical, then there must be a member mx such that Tx <: Ti, 1 ≤ x ≤ k for
/// all i ∈ 1..k, or a static type warning occurs. The member that is inherited
/// is mx, if it exists; otherwise: let numberOf P ositionals(f) denote the
/// number of positional parameters of a function f, and let numberOf
/// RequiredParams(f) denote the number of required parameters of a function f.
/// Furthermore, let s denote the set of all named parameters of the m1,...,mk.
/// Then let
/// h = max(numberOf Positionals(mi)),
/// r = min(numberOf RequiredParams(mi)), i ∈ 1..k.
/// Then I has a method named n, with r required parameters of type dynamic,
/// h positional parameters of type dynamic, named parameters s of type dynamic
/// and return type dynamic.
/// @description Checks that there's no static warning produced when the
/// subinterface member being accessed is actually inherited. Members tested
/// include variables, methods, getters, setters and operators. There's no way
/// to check whether the resulting inherited method signature is actually as
/// described above, but we can check that it's inherited at all. Test type
/// aliases
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class C {}
class D extends C {}

abstract class SI1 {
  int method(num? v, String? p, {int? o1, Pattern? o2});
  int method2(C? v, [D? o]);
  int? get gett0r;
  void set sett0r(int? v);
  C operator +(C v);
}

abstract class SI2 {
  void method(num? v, String? p, {int? o1, Pattern? o2});
  num method2(C? v, [D? o]);
  num? get gett0r;
  void set sett0r(num? v);
  C operator +(C v);
}

typedef SIAlias1 = SI1;
typedef SIAlias2 = SI2;

abstract class I implements SIAlias1, SIAlias2, C {}

main() {
  I? i = null;

  i?.method(null, null, o1:null, o2:null);
  var v = i?.method2(null, null);
  num? n = i?.gett0r;
  i?.sett0r = null;
  Expect.throws(() {var v = (i as I) + (i as I);});
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > inheritance_t14', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// Otherwise, if the static types T1,...,Tk of the members m1,...,mk are not
/// identical, then there must be a member mx such that Tx <: Ti, 1 ≤ x ≤ k for
/// all i ∈ 1..k, or a static type warning occurs. The member that is inherited
/// is mx, if it exists; otherwise: let numberOf P ositionals(f) denote the
/// number of positional parameters of a function f, and let numberOf
/// RequiredParams(f) denote the number of required parameters of a function f.
/// Furthermore, let s denote the set of all named parameters of the m1,...,mk.
/// Then let
/// h = max(numberOf Positionals(mi)),
/// r = min(numberOf RequiredParams(mi)), i ∈ 1..k.
/// Then I has a method named n, with r required parameters of type dynamic,
/// h positional parameters of type dynamic, named parameters s of type dynamic
/// and return type dynamic.
/// @description Checks that there's no static warning if class implements two
/// interfaces with same named methods and different number of required formal
/// parameters and defines method with minimum number of required parameters
/// and maximum number of positional parameters. Test type aliases
/// @author sgrekhov@unipro.ru

abstract class SI1 {
  void foo(var v);
}

abstract class SI2 {
  void foo(var v, var vv);
}

typedef SIAlias1 = SI1;
typedef SIAlias2 = SI2;

class I implements SIAlias1, SIAlias2 {
  // It is expected that I inherits void foo(dynamic v1, [dynamic a])
  void foo(dynamic v, [dynamic a]) {}
}

main() {
  I i = new I();
  i.foo(1);
  i.foo(1,2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > not_overriden_members_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2011, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion An interface I inherits any members of its superinterfaces that
/// are not overridden by members declared in I.
/// @description Checks that there're no static warnings produced when
/// attempting to access the inherited non-static members of a null variable
/// whose static type is an interface extending another interface that declares
/// those members. This should indirectly prove that they're indeed inherited
/// by the subinterface.
/// @author sgrekhov@unopro.ru

import 'package:co19_expect/expect.dart';

abstract class I {
  void m();
  int get gett0r;
  void set sett0r(int v);
  I operator+(int i);
}

abstract class C implements I {
}

main() {
  C? c = null;

  c?.m();
  c?.gett0r;
  c?.sett0r = 1;
  Expect.throws(() {(c as C) + 1;});
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > not_overriden_members_t03', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion An interface I inherits any members of its superinterfaces that
/// are not overridden by members declared in I.
/// @description Checks that there're no static warnings produced when
/// attempting to access the inherited non-static members of a null variable
/// whose static type is an interface extending another interface that declares
/// those members. This should indirectly prove that they're indeed inherited
/// by the subinterface. Test type aliases
/// @author sgrekhov@unopro.ru

import 'package:co19_expect/expect.dart';

abstract class I {
  void m();
  int get gett0r;
  void set sett0r(int v);
  I operator+(int i);
}

typedef IAlias = I;

abstract class C implements IAlias {
}

main() {
  C? c = null;

  c?.m();
  c?.gett0r;
  c?.sett0r = 1;
  Expect.throws(() {(c as C) + 1;});
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > same_name_getters_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2011, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// However, if the above rules would cause multiple members m1,...,mk with
/// the same name n to be inherited (because identically named members existed
/// in several superinterfaces) then at most one member is inherited
/// @description Checks that if multiple getters with the same name are
/// inherited actually only one getter is inherited and no static warning
/// issued
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

abstract class SI1 {
  int get m => 1;
}

abstract class SI2 {
  int get m => 2;
}

abstract class I implements SI1, SI2 {
}

class C extends I {
  int get m => 3;
}

main() {
  I i = new C();
  Expect.equals(3, i.m, "Wrong getter inherited");
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > same_name_getters_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2011, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// However, if the above rules would cause multiple members m1,...,mk with
/// the same name n to be inherited (because identically named members existed
/// in several superinterfaces) then at most one member is inherited
/// @description Checks that if multiple methods with the same name are
/// inherited actually only one method is inherited and no static warning
/// issued
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

abstract class SI1 {
  int m() {
    return 1;
  }
}

abstract class SI2 {
  int m() {
    return 2;
  }
}

abstract class I implements SI1, SI2 {
}

class C extends I {
  int m() {
    return 3;
  }
}

main() {
  I i = new C();
  Expect.equals(3, i.m(), "Wrong method inherited");
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > same_name_getters_t03', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// However, if the above rules would cause multiple members m1,...,mk with
/// the same name n to be inherited (because identically named members existed
/// in several superinterfaces) then at most one member is inherited
/// @description Checks that if multiple getters with the same name are
/// inherited actually only one getter is inherited and no static warning
/// issued. Test type aliases
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

abstract class SI1 {
  int get m => 1;
}

abstract class SI2 {
  int get m => 2;
}

typedef SIAlias1 = SI1;
typedef SIAlias2 = SI2;

abstract class I implements SIAlias1, SIAlias2 {
}

class C extends I {
  int get m => 3;
}

main() {
  I i = new C();
  Expect.equals(3, i.m, "Wrong getter inherited");
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > same_name_getters_t04', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// However, if the above rules would cause multiple members m1,...,mk with
/// the same name n to be inherited (because identically named members existed
/// in several superinterfaces) then at most one member is inherited
/// @description Checks that if multiple methods with the same name are
/// inherited actually only one method is inherited and no static warning
/// issued. Test type aliases
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

abstract class SI1 {
  int m() {
    return 1;
  }
}

abstract class SI2 {
  int m() {
    return 2;
  }
}

typedef SIAlias1 = SI1;
typedef SIAlias2 = SI2;

abstract class I implements SIAlias1, SIAlias2 {
}

class C extends I {
  int m() {
    return 3;
  }
}

main() {
  I i = new C();
  Expect.equals(3, i.m(), "Wrong method inherited");
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > same_name_getters_type_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2011, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// If some but not all of the mi, 1 ≤ i ≤ k are getters none of the mi are
/// inherited, and a static warning is issued.
/// Otherwise, if the static types T1,...,Tk of the members m1,...,mk are not
/// identical, then there must be a member mx such that Tx <: Ti, 1 ≤ x ≤ k for
/// all i ∈ 1..k, or a static type warning occurs.
/// @description Checks that if all inherited members with the same name are
/// getters and their static types are identical, then no static warning issued
/// @author sgrekhov@unipro.ru


abstract class SI1 {
  int get m => 1;
}

abstract class SI2 {
  int get m => 2;
}

abstract class SI3 {
  int get m;
}

abstract class I implements SI1, SI2, SI3 {
}

main() {
  I? i = null;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > same_name_getters_type_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2011, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// If some but not all of the mi, 1 ≤ i ≤ k are getters none of the mi are
/// inherited, and a static warning is issued.
/// Otherwise, if the static types T1,...,Tk of the members m1,...,mk are not
/// identical, then there must be a member mx such that Tx <: Ti, 1 ≤ x ≤ k for
/// all i ∈ 1..k, or a static type warning occurs.
/// @description Checks that if all inherited members with the same name are
/// methods and their static types are identical, then no static warning issued
/// @author sgrekhov@unipro.ru


abstract class SI1 {
  int m() {
    return 1;
  }
}

abstract class SI2 {
  int m() {
    return 2;
  }
}

abstract class SI3 {
  int m();
}

abstract class I implements SI1, SI2, SI3 {
}

main() {
  I? i = null;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > same_name_getters_type_t09', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2011, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// If some but not all of the mi, 1 ≤ i ≤ k are getters none of the mi are
/// inherited, and a static warning is issued.
/// Otherwise, if the static types T1,...,Tk of the members m1,...,mk are not
/// identical, then there must be a member mx such that Tx <: Ti, 1 ≤ x ≤ k for
/// all i ∈ 1..k, or a static type warning occurs.
/// @description Checks that if all inherited members with the same name are
/// methods and their static types are not identical, but there is a method
/// such that its type is subtype of types of all other methods then no static
/// warning issued. Test methods with parameters and return type void (except
/// the subtype)
/// @author sgrekhov@unipro.ru


abstract class SI1 {
  void m1(int v1);
}

abstract class SI2 {
  void m1(double v1);
}

abstract class SI3 {
  int m1(num v1);
}

abstract class I implements SI1, SI2, SI3 {
}

main() {
  I? i = null;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > same_name_getters_type_t10', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// If some but not all of the mi, 1 ≤ i ≤ k are getters none of the mi are
/// inherited, and a static warning is issued.
/// Otherwise, if the static types T1,...,Tk of the members m1,...,mk are not
/// identical, then there must be a member mx such that Tx <: Ti, 1 ≤ x ≤ k for
/// all i ∈ 1..k, or a static type warning occurs.
/// @description Checks that if all inherited members with the same name are
/// getters and their static types are identical, then no static warning issued.
/// Test type aliases
/// @author sgrekhov@unipro.ru

abstract class SI1 {
  int get m => 1;
}

abstract class SI2 {
  int get m => 2;
}

abstract class SI3 {
  int get m;
}

typedef SIAlias1 = SI1;
typedef SIAlias2 = SI2;
typedef SIAlias3 = SI3;

abstract class I implements SIAlias1, SIAlias2, SIAlias3 {
}

main() {
  I? i = null;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > same_name_getters_type_t11', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// If some but not all of the mi, 1 ≤ i ≤ k are getters none of the mi are
/// inherited, and a static warning is issued.
/// Otherwise, if the static types T1,...,Tk of the members m1,...,mk are not
/// identical, then there must be a member mx such that Tx <: Ti, 1 ≤ x ≤ k for
/// all i ∈ 1..k, or a static type warning occurs.
/// @description Checks that if all inherited members with the same name are
/// methods and their static types are identical, then no static warning issued.
/// Test type aliases
/// @author sgrekhov@unipro.ru

abstract class SI1 {
  int m() {
    return 1;
  }
}

abstract class SI2 {
  int m() {
    return 2;
  }
}

abstract class SI3 {
  int m();
}

typedef SIAlias1 = SI1;
typedef SIAlias2 = SI2;
typedef SIAlias3 = SI3;

abstract class I implements SIAlias1, SIAlias2, SIAlias3 {
}

main() {
  I? i = null;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > Inheritance_and_Overriding > same_name_members_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2011, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion We define inherited(J, K) to be
/// the set of members m such that all of the following hold:
/// • m is accessible to K and
/// • A is a direct superinterface of J and either
/// – A declares a member m or
/// – m is a member of inherited(A, K).
/// • m is not overridden by J.
/// ...
/// If the above rules would cause multiple members m1 , . . . , mk with
/// the same name n to be inherited (because identically named members existed
/// in several superinterfaces) then at most one member is inherited.
/// If some but not all of the mi , 1 ≤ i ≤ k are getters none of the mi are
/// inherited, and a static warning is issued.
/// @description Checks that there is no static warning if multiple members
/// with the same name are inherited and all of them are getters or all of
/// them are methods.
/// @author ngl@unipro.ru


class A1 {
  int get a => 1;
  int m() { return 11; }
}

class A2 {
  int get a => 2;
  int m() { return 12; }
}

class A3 {
  int get a => 3;
  int m() { return 13; }
}

abstract class I implements A1, A2, A3 {
}

main() {
  I? i = null;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Superinterfaces > definition_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2011, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion An interface has a set of direct superinterfaces.
/// An interface J is a superinterface of an interface I iff either J is a
/// direct superinterface of I or J is a superinterface of a direct
/// superinterface of I.
/// @description Checks that an interface can declare generic and non-generic
/// direct superinterfaces or not declare any.
/// @author vasya


abstract class J {}
abstract class I implements J {}

abstract class J1 {}
abstract class J2 implements J1 {}
abstract class J3 implements J2 {}
abstract class I1 implements J1, J2, J3 {}
abstract class I2 implements J3, J2, J1 {}

abstract class K<S> {}
abstract class L<S> implements K<S> {}

abstract class A<S, T> {}
abstract class B<S, T> implements A<T, S> {}
abstract class C<Q, R, S> implements A<Q, S> {}
abstract class D<Q> implements A<int, Q>, J, I {}

main() {
  I? i = null;
  I1? i1 = null;
  I2? i2 = null;
  B<String, int>? b = null;
  C<List, String, num>? c = null;
  D<String>? d = null;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
  });
}
