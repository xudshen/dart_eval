// Generated by co19_adapter — do not edit manually.
// ignore_for_file: unused_import

import 'package:dart_eval/dart_eval.dart';
import 'package:test/test.dart';

import '../_infrastructure/expect_plugin.dart';

void main() {
  group('co19 Language/Mixins', () {
    test('Mixin_Application > implicit_constructor_A01_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// Sq(T1 a1, . . ., Tk ak)
/// of S that is accessible to LC, C has an implicitly declared constructor of
/// the form
/// Cq(T1 a1, ..., Tk ak): superq(a1, . . ., ak);
/// where Cq is obtained from Sq by replacing occurrences of SN, which denote
/// the superclass, by N, and superq is obtained from Sq by replacing
/// occurrences of SN which denote the superclass by super.
///
/// @description Checks that for each generative constructor of the form
/// Sq(T1 a1, . . ., Tk ak) a mixin application declares constructor of the form
/// Cq(T1 a1, ..., Tk ak): superq(a1, . . ., ak);
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  bool v1;
  num v2;
  A(bool this.v1, num this.v2);
}

mixin class M1 {
}

class C = A with M1;

main() {
  C c = new C(true, 2);
  Expect.equals(true, c.v1);
  Expect.equals(2, c.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A01_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// Sq(T1 a1, . . ., Tk ak)
/// of S that is accessible to LC, C has an implicitly declared constructor of
/// the form
/// Cq(T1 a1, ..., Tk ak): superq(a1, . . ., ak);
/// where Cq is obtained from Sq by replacing occurrences of SN, which denote
/// the superclass, by N, and superq is obtained from Sq by replacing
/// occurrences of SN which denote the superclass by super.
///
/// @description Checks that for each generative constructor of the form
/// Sq(T1 a1, . . ., Tk ak) a mixin application declares constructor of the form
/// Cq(T1 a1, ..., Tk ak): superq(a1, . . ., ak);
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  bool v1;
  num v2;
  A(bool this.v1, num this.v2);
}

mixin class M1 {
  num v2 = -1;
}

class C = A with M1;

main() {
  C c = new C(true, 2);
  Expect.equals(true, c.v1);
  Expect.equals(-1, c.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A01_t03', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// Sq(T1 a1, . . ., Tk ak)
/// of S that is accessible to LC, C has an implicitly declared constructor of
/// the form
/// Cq(T1 a1, ..., Tk ak): superq(a1, . . ., ak);
/// where Cq is obtained from Sq by replacing occurrences of SN, which denote
/// the superclass, by N, and superq is obtained from Sq by replacing
/// occurrences of SN which denote the superclass by super.
///
/// @description Checks that for each generative constructor of the form
/// Sq(T1 a1, . . ., Tk ak) a mixin application declares constructor of the form
/// Cq(T1 a1, ..., Tk ak): superq(a1, . . ., ak);. Test type aliases
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  bool v1;
  num v2;
  A(bool this.v1, num this.v2);
}

typedef AAlias = A;

mixin class M1 {
}

class C = AAlias with M1;

main() {
  C c = new C(true, 2);
  Expect.equals(true, c.v1);
  Expect.equals(2, c.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A01_t04', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// Sq(T1 a1, . . ., Tk ak)
/// of S that is accessible to LC, C has an implicitly declared constructor of
/// the form
/// Cq(T1 a1, ..., Tk ak): superq(a1, . . ., ak);
/// where Cq is obtained from Sq by replacing occurrences of SN, which denote
/// the superclass, by N, and superq is obtained from Sq by replacing
/// occurrences of SN which denote the superclass by super.
///
/// @description Checks that for each generative constructor of the form
/// Sq(T1 a1, . . ., Tk ak) a mixin application declares constructor of the form
/// Cq(T1 a1, ..., Tk ak): superq(a1, . . ., ak);. Test type aliases
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  bool v1;
  num v2;
  A(bool this.v1, num this.v2);
}

typedef AAlias = A;

mixin class M1 {
  num v2 = -1;
}

class C = AAlias with M1;

main() {
  C c = new C(true, 2);
  Expect.equals(true, c.v1);
  Expect.equals(-1, c.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A01_t05', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2024, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// `Sq(T1 a1, . . ., Tk ak)` of `S` that is accessible to `LC`, `C` has an
/// implicitly declared constructor of the form
/// `Cq(T1 a1, ..., Tk ak): superq(a1, . . ., ak);` where `Cq` is obtained from
/// `Sq` by replacing occurrences of `SN`, which denote the superclass, by `N`,
/// and `superq` is obtained from `Sq` by replacing occurrences of `SN` which
/// denote the superclass by super. If `Sq` is a generative const constructor,
/// and `C` does not declare any instance variables, `Cq` is also a const
/// constructor.
///
/// @description Checks that if `Sq` is a generative const constructor, and `C`
/// does not declare any instance variables then `Cq` is also a const
/// constructor.
/// @author sgrekhov22@gmail.com
/// @issue 59796

import 'package:co19_expect/expect.dart';

class A {
  final bool v1;
  final num v2;
  const A(this.v1, this.v2);
}

mixin M on A {
  static int x = 0;
}

class MA = A with M;

main() {
  MA ma = const MA(true, 2);
  Expect.equals(true, ma.v1);
  Expect.equals(2, ma.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A01_t06', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2024, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// `Sq(T1 a1, . . ., Tk ak)` of `S` that is accessible to `LC`, `C` has an
/// implicitly declared constructor of the form
/// `Cq(T1 a1, ..., Tk ak): superq(a1, . . ., ak);` where `Cq` is obtained from
/// `Sq` by replacing occurrences of `SN`, which denote the superclass, by `N`,
/// and `superq` is obtained from `Sq` by replacing occurrences of `SN` which
/// denote the superclass by super. If `Sq` is a generative const constructor,
/// and `C` does not declare any instance variables, `Cq` is also a const
/// constructor.
///
/// @description Checks that if `Sq` is a generative const constructor, and `C`
/// does not declare any instance variables then `Cq` is also a const
/// constructor.
/// @author sgrekhov22@gmail.com
/// @issue 59796

import 'package:co19_expect/expect.dart';

class A {
  final String v1;
  final num v2;
  const A(this.v1, this.v2);
}

mixin M on A {
  static int x = 0;
}

class MA extends A with M {
  const MA(String v1, num v2) : super(v1, v2);
}

main() {
  MA ma = const MA("x", 3);
  Expect.equals("x", ma.v1);
  Expect.equals(3, ma.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A02_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// Sq(T1 a1, ..., Tk ak, [Tk+1 ak+1 = d1, ..., Tk+p ak+p = dp]) of S that is
/// accessible to LC, C has an implicitly declared constructor of the form
/// Cq(T1 a1, ... , Tk ak, [Tk+1 ak+1 = d′1, ... , Tk+p ak+p = d′p]) :
///     superq(a1, ... , ak, ak+1, ..., ap);
/// where Cq is obtained from Sq by replacing occurrences of SN , which denote
/// the superclass, by N, superq is obtained from Sq by replacing occurrences of
/// SN which denote the superclass by super, and d′i, i ∈ 1..p, is a constant
/// expression evaluating to the same value as di.
///
/// @description Checks that for each generative constructor of the form
/// Sq(T1 a1, . . ., Tk ak, [Tk+1 ak+1 = d1, ..., Tk+p ak+p = dp]) a mixin
/// application implicitly declares constructor of the form
/// Cq(T1 a1, ... , Tk ak, [Tk+1 ak+1 = d′1, ... , Tk+p ak+p = d′p]) :
///     superq(a1, ... , ak, ak+1, ..., ap);
/// @author sgrekhov22@gmail.com

import 'package:co19_expect/expect.dart';

class A {
  bool v1;
  num v2;
  A(bool this.v1, [num this.v2 = 3.14]);
}

mixin class M1 {}

class C = A with M1;

main() {
  C c1 = C(true, 2);
  Expect.isTrue(c1.v1);
  Expect.equals(2, c1.v2);

  C c2 = C(false);
  Expect.isFalse(c2.v1);
  Expect.equals(3.14, c2.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A02_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// Sq(T1 a1, ..., Tk ak, [Tk+1 ak+1 = d1, ..., Tk+p ak+p = dp]) of S that is
/// accessible to LC, C has an implicitly declared constructor of the form
/// Cq(T1 a1, ... , Tk ak, [Tk+1 ak+1 = d′1, ... , Tk+p ak+p = d′p]) :
///     superq(a1, ... , ak, ak+1, ..., ap);
/// where Cq is obtained from Sq by replacing occurrences of SN , which denote
/// the superclass, by N, superq is obtained from Sq by replacing occurrences of
/// SN which denote the superclass by super, and d′i, i ∈ 1..p, is a constant
/// expression evaluating to the same value as di.
///
/// @description Checks that for each generative constructor of the form
/// Sq(T1 a1, . . ., Tk ak, [Tk+1 ak+1 = d1, ..., Tk+p ak+p = dp]) a mixin
/// application implicitly declares constructor of the form
/// Cq(T1 a1, ... , Tk ak, [Tk+1 ak+1 = d′1, ... , Tk+p ak+p = d′p]) :
///     superq(a1, ... , ak, ak+1, ..., ap);
/// @author sgrekhov22@gmail.com

import 'package:co19_expect/expect.dart';

class A {
  bool v1;
  num v2;
  A(bool this.v1, [num this.v2 = 3.14]);
}

mixin class M1 {
  num v2 = -1;
}

class C = A with M1;

main() {
  C c1 = C(true, 2);
  Expect.isTrue(c1.v1);
  Expect.equals(-1, c1.v2);

  C c2 = C(false);
  Expect.isFalse(c2.v1);
  Expect.equals(-1, c2.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A02_t03', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2024, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// `Sq(T1 a1, ..., Tk ak, [Tk+1 ak+1 = d1, ..., Tk+p ak+p = dp])` of `S` that is
/// accessible to `LC`, `C` has an implicitly declared constructor of the form
/// `Cq(T1 a1, ... , Tk ak, [Tk+1 ak+1 = d′1, ... , Tk+p ak+p = d′p]) :
///     superq(a1, ... , ak, ak+1, ..., ap);`
/// where `Cq` is obtained from `Sq` by replacing occurrences of `SN`, which
/// denote the superclass, by `N`, `superq` is obtained from `Sq` by replacing
/// occurrences of `SN` which denote the superclass by super, and
/// `d′i, i ∈ 1..p`, is a constant expression evaluating to the same value as
/// `di`. If `Sq` is a generative const constructor, and `MC` does not declare
/// any instance variables, `Cq` is also a const constructor.
///
/// @description Checks that if `Sq` is a generative const constructor, and `MC`
/// does not declare any instance variables, `Cq` is also a const constructor.
/// @author sgrekhov22@gmail.com
/// @issue 59796

import 'package:co19_expect/expect.dart';

class A {
  final bool v1;
  final num v2;
  const A(bool this.v1, [num this.v2 = 3.14]);
}

mixin class M {
  static int x = 0;
}

class MA = A with M;

main() {
  MA ma = const MA(true, 2);
  Expect.isTrue(ma.v1);
  Expect.equals(2, ma.v2);

  ma = const MA(false);
  Expect.isFalse(ma.v1);
  Expect.equals(3.14, ma.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A02_t04', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2024, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// `Sq(T1 a1, ..., Tk ak, [Tk+1 ak+1 = d1, ..., Tk+p ak+p = dp])` of `S` that is
/// accessible to `LC`, `C` has an implicitly declared constructor of the form
/// `Cq(T1 a1, ... , Tk ak, [Tk+1 ak+1 = d′1, ... , Tk+p ak+p = d′p]) :
///     superq(a1, ... , ak, ak+1, ..., ap);`
/// where `Cq` is obtained from `Sq` by replacing occurrences of `SN`, which
/// denote the superclass, by `N`, `superq` is obtained from `Sq` by replacing
/// occurrences of `SN` which denote the superclass by super, and
/// `d′i, i ∈ 1..p`, is a constant expression evaluating to the same value as
/// `di`. If `Sq` is a generative const constructor, and `MC` does not declare
/// any instance variables, `Cq` is also a const constructor.
///
/// @description Checks that if `Sq` is a generative const constructor, and `MC`
/// does not declare any instance variables, `Cq` is also a const constructor.
/// @author sgrekhov22@gmail.com
/// @issue 59796

import 'package:co19_expect/expect.dart';

class A {
  final bool v1;
  final num v2;
  const A(bool this.v1, [num this.v2 = 3.14]);
}

mixin class M {
  static int x = 0;
}

class MA extends A with M {
  const MA(bool v1, [num v2 = 0]) : super(v1, v2);
}

main() {
  MA ma = const MA(true, 2);
  Expect.isTrue(ma.v1);
  Expect.equals(2, ma.v2);

  ma = const MA(false);
  Expect.isFalse(ma.v1);
  Expect.equals(0, ma.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A03_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// Sq(T1 a1, ..., Tk ak, {Tk+1 ak+1 = d1, ..., Tk+n ak+n = dn}) of S that is
/// accessible to LC, C has an implicitly declared constructor of the form
/// Cq(T1 a1, ... , Tk ak, {Tk+1 ak+1 = d′1, ... , Tk+n ak+n = d′n})
///     : superq(a1, ... , ak, ak+1: ak+1, ..., ap: ap);
/// where Cq is obtained from Sq by replacing occurrences of SN which denote the
/// superclass by N, superq is obtained from Sq by replacing occurrences of SN
/// which denote the superclass by super, and d′i, i ∈ 1..n, is a constant
/// expression evaluating to the same value as di.
///
/// @description Checks that for each generative constructor of the form
/// Sq(T1 a1, ..., Tk ak, {Tk+1 ak+1 = d1, ..., Tk+n ak+n = dn}) a mixin
/// application implicitly declares constructor of the form
/// Cq(T1 a1, ... , Tk ak, {Tk+1 ak+1 = d′1, ... , Tk+n ak+n = d′n})
///     : superq(a1, ... , ak, ak+1: ak+1, ..., ap: ap);
/// @author sgrekhov22@gmail.com

import 'package:co19_expect/expect.dart';

class A {
  bool v1;
  num v2;
  A(bool this.v1, {num this.v2 = 3.14});
}

mixin class M1 {}

class C = A with M1;

main() {
  C c1 = C(true, v2: 2);
  Expect.isTrue(c1.v1);
  Expect.equals(2, c1.v2);

  C c2 = C(false);
  Expect.isFalse(c2.v1);
  Expect.equals(3.14, c2.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A03_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// Sq(T1 a1, ..., Tk ak, [Tk+1 ak+1 = d1, ..., Tk+p ak+p = dp]) of S that is
/// accessible to LC, C has an implicitly declared constructor of the form
/// Cq(T1 a1, ... , Tk ak, [Tk+1 ak+1 = d′1, ... , Tk+p ak+p = d′p]) :
///     superq(a1, ... , ak, ak+1, ..., ap);
/// where Cq is obtained from Sq by replacing occurrences of SN , which denote
/// the superclass, by N, superq is obtained from Sq by replacing occurrences of
/// SN which denote the superclass by super, and d′i, i ∈ 1..p, is a constant
/// expression evaluating to the same value as di.
///
/// @description Checks that for each generative constructor of the form
/// Sq(T1 a1, . . ., Tk ak, [Tk+1 ak+1 = d1, ..., Tk+p ak+p = dp]) a mixin
/// application implicitly declares constructor of the form
/// Cq(T1 a1, ... , Tk ak, [Tk+1 ak+1 = d′1, ... , Tk+p ak+p = d′p]) :
///     superq(a1, ... , ak, ak+1, ..., ap);
/// @author sgrekhov22@gmail.com

import 'package:co19_expect/expect.dart';

class A {
  bool v1;
  num v2;
  A(bool this.v1, {num this.v2 = 3.14});
}

mixin class M1 {
  num v2 = -1;
}

class C = A with M1;

main() {
  C c1 = C(true, v2: 2);
  Expect.isTrue(c1.v1);
  Expect.equals(-1, c1.v2);

  C c2 = C(false);
  Expect.isFalse(c2.v1);
  Expect.equals(-1, c2.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A03_t03', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// Sq(T1 a1, ..., Tk ak, {Tk+1 ak+1 = d1, ..., Tk+n ak+n = dn}) of S that is
/// accessible to LC, C has an implicitly declared constructor of the form
/// Cq(T1 a1, ... , Tk ak, {Tk+1 ak+1 = d′1, ... , Tk+n ak+n = d′n})
///     : superq(a1, ... , ak, ak+1: ak+1, ..., ap: ap);
/// where Cq is obtained from Sq by replacing occurrences of SN which denote the
/// superclass by N, superq is obtained from Sq by replacing occurrences of SN
/// which denote the superclass by super, and d′i, i ∈ 1..n, is a constant
/// expression evaluating to the same value as di.
///
/// @description Checks that for each generative constructor of the form
/// Sq(T1 a1, ..., Tk ak, {Tk+1 ak+1 = d1, ..., Tk+n ak+n = dn}) a mixin
/// application implicitly declares constructor of the form
/// Cq(T1 a1, ... , Tk ak, {Tk+1 ak+1 = d′1, ... , Tk+n ak+n = d′n})
///     : superq(a1, ... , ak, ak+1: ak+1, ..., ap: ap);
/// @author sgrekhov22@gmail.com

import 'package:co19_expect/expect.dart';

class A {
  bool v1;
  num v2;
  A(bool this.v1, {required num this.v2});
}

mixin class M1 {}

class C = A with M1;

main() {
  C c1 = C(true, v2: 2);
  Expect.isTrue(c1.v1);
  Expect.equals(2, c1.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A03_t04', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// Sq(T1 a1, ..., Tk ak, [Tk+1 ak+1 = d1, ..., Tk+p ak+p = dp]) of S that is
/// accessible to LC, C has an implicitly declared constructor of the form
/// Cq(T1 a1, ... , Tk ak, [Tk+1 ak+1 = d′1, ... , Tk+p ak+p = d′p]) :
///     superq(a1, ... , ak, ak+1, ..., ap);
/// where Cq is obtained from Sq by replacing occurrences of SN , which denote
/// the superclass, by N, superq is obtained from Sq by replacing occurrences of
/// SN which denote the superclass by super, and d′i, i ∈ 1..p, is a constant
/// expression evaluating to the same value as di.
///
/// @description Checks that for each generative constructor of the form
/// Sq(T1 a1, . . ., Tk ak, [Tk+1 ak+1 = d1, ..., Tk+p ak+p = dp]) a mixin
/// application implicitly declares constructor of the form
/// Cq(T1 a1, ... , Tk ak, [Tk+1 ak+1 = d′1, ... , Tk+p ak+p = d′p]) :
///     superq(a1, ... , ak, ak+1, ..., ap);
/// @author sgrekhov22@gmail.com

import 'package:co19_expect/expect.dart';

class A {
  bool v1;
  num v2;
  A(bool this.v1, {required num this.v2});
}

mixin class M1 {
  num v2 = -1;
}

class C = A with M1;

main() {
  C c1 = C(true, v2: 2);
  Expect.isTrue(c1.v1);
  Expect.equals(-1, c1.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A03_t06', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2024, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// `Sq(T1 a1, ..., Tk ak, {Tk+1 ak+1 = d1, ..., Tk+n ak+n = dn})` of `S` that
/// is accessible to `LC`, `C` has an implicitly declared constructor of the
/// form `Cq(T1 a1, ... , Tk ak, {Tk+1 ak+1 = d′1, ... , Tk+n ak+n = d′n})
///     : superq(a1, ... , ak, ak+1: ak+1, ..., ap: ap);`
/// where `Cq` is obtained from `Sq` by replacing occurrences of `SN` which
/// denote the superclass by `N`, `superq` is obtained from `Sq` by replacing
/// occurrences of `SN` which denote the superclass by super, and
/// `d′i, i ∈ 1..n`, is a constant expression evaluating to the same value as
/// `di`. If `Sq` is a generative const constructor, and `M` does not declare
/// any fields, `Cq` is also a const constructor.
///
/// @description Checks that if `Sq` is a generative const constructor, and `M`
/// does not declare any instance variables, `Cq` is also a const constructor.
/// @author sgrekhov22@gmail.com
/// @issue 59796

import 'package:co19_expect/expect.dart';

class A {
  final bool v1;
  final num v2;
  const A(bool this.v1, {num this.v2 = 3.14});
}

mixin M {
  static int x = 0;
}

class MA = A with M;

main() {
  MA ma = const MA(true, v2: 2);
  Expect.isTrue(ma.v1);
  Expect.equals(2, ma.v2);

  ma = const MA(false);
  Expect.isFalse(ma.v1);
  Expect.equals(3.14, ma.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A03_t07', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2024, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// `Sq(T1 a1, ..., Tk ak, {Tk+1 ak+1 = d1, ..., Tk+n ak+n = dn})` of `S` that
/// is accessible to `LC`, `C` has an implicitly declared constructor of the
/// form `Cq(T1 a1, ... , Tk ak, {Tk+1 ak+1 = d′1, ... , Tk+n ak+n = d′n})
///     : superq(a1, ... , ak, ak+1: ak+1, ..., ap: ap);`
/// where `Cq` is obtained from `Sq` by replacing occurrences of `SN` which
/// denote the superclass by `N`, `superq` is obtained from `Sq` by replacing
/// occurrences of `SN` which denote the superclass by super, and
/// `d′i, i ∈ 1..n`, is a constant expression evaluating to the same value as
/// `di`. If `Sq` is a generative const constructor, and `M` does not declare
/// any fields, `Cq` is also a const constructor.
///
/// @description Checks that if `Sq` is a generative const constructor, and `M`
/// does not declare any instance variables, `Cq` is also a const constructor.
/// @author sgrekhov22@gmail.com
/// @issue 59796

import 'package:co19_expect/expect.dart';

class A {
  final bool v1;
  final num v2;
  const A(bool this.v1, {required num this.v2});
}

mixin class M {
  static int x = 0;
}

class MA = A with M;

main() {
  MA ma = const MA(true, v2: 2);
  Expect.isTrue(ma.v1);
  Expect.equals(2, ma.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A03_t08', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2024, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// `Sq(T1 a1, ..., Tk ak, {Tk+1 ak+1 = d1, ..., Tk+n ak+n = dn})` of `S` that
/// is accessible to `LC`, `C` has an implicitly declared constructor of the
/// form `Cq(T1 a1, ... , Tk ak, {Tk+1 ak+1 = d′1, ... , Tk+n ak+n = d′n})
///     : superq(a1, ... , ak, ak+1: ak+1, ..., ap: ap);`
/// where `Cq` is obtained from `Sq` by replacing occurrences of `SN` which
/// denote the superclass by `N`, `superq` is obtained from `Sq` by replacing
/// occurrences of `SN` which denote the superclass by super, and
/// `d′i, i ∈ 1..n`, is a constant expression evaluating to the same value as
/// `di`. If `Sq` is a generative const constructor, and `M` does not declare
/// any fields, `Cq` is also a const constructor.
///
/// @description Checks that if `Sq` is a generative const constructor, and `M`
/// does not declare any instance variables, `Cq` is also a const constructor.
/// @author sgrekhov22@gmail.com
/// @issue 59796

import 'package:co19_expect/expect.dart';

class A {
  final bool v1;
  final num v2;
  const A(bool this.v1, {num this.v2 = 3.14});
}

mixin class M {
  static int x = 0;
}

class MA extends A with M {
  const MA(bool v1, {num v2 = 0}) : super(v1, v2: v2);
}

main() {
  MA ma = const MA(true, v2: 2);
  Expect.isTrue(ma.v1);
  Expect.equals(2, ma.v2);

  ma = const MA(false);
  Expect.isFalse(ma.v1);
  Expect.equals(0, ma.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > implicit_constructor_A03_t09', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2024, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion For each generative constructor of the form
/// `Sq(T1 a1, ..., Tk ak, {Tk+1 ak+1 = d1, ..., Tk+n ak+n = dn})` of `S` that
/// is accessible to `LC`, `C` has an implicitly declared constructor of the
/// form `Cq(T1 a1, ... , Tk ak, {Tk+1 ak+1 = d′1, ... , Tk+n ak+n = d′n})
///     : superq(a1, ... , ak, ak+1: ak+1, ..., ap: ap);`
/// where `Cq` is obtained from `Sq` by replacing occurrences of `SN` which
/// denote the superclass by `N`, `superq` is obtained from `Sq` by replacing
/// occurrences of `SN` which denote the superclass by super, and
/// `d′i, i ∈ 1..n`, is a constant expression evaluating to the same value as
/// `di`. If `Sq` is a generative const constructor, and `M` does not declare
/// any fields, `Cq` is also a const constructor.
///
/// @description Checks that if `Sq` is a generative const constructor, and `M`
/// does not declare any instance variables, `Cq` is also a const constructor.
/// @author sgrekhov22@gmail.com

import 'package:co19_expect/expect.dart';

class A {
  final bool v1;
  final num v2;
  const A(bool this.v1, {required num this.v2});
}

mixin class M {}

class MA extends A with M {
  const MA(bool v1, {required num v2}) : super(v1, v2: v2);
}

main() {
  MA ma = const MA(true, v2: 2);
  Expect.isTrue(ma.v1);
  Expect.equals(2, ma.v2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > initializers_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion If any of the instance fields of M (respectively, Mk) have
/// initializers, they are executed in the scope of M (respectively, Mk) to
/// initialize the corresponding fields of C.
///
/// @description Checks that if mixins have initializers then they are executed
/// and initialize corresponding fields of the class
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int _xa = 0;
  int get x => _xa;
}

mixin class M1 {
  int _xm1 = 1;
  int get x => _xm1;
}

mixin class M2 {
  int _xm2 = 2;
  int get x => _xm2;
}

class B1 = A with M1;
class B2 = A with M1, M2;
class B3 = B1 with M2;

main() {
  B1 b1 = new B1();
  B2 b2 = new B2();
  B3 b3 = new B3();

  Expect.equals(1, b1.x);
  Expect.equals(2, b2.x);
  Expect.equals(2, b3.x);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > initializers_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion If any of the instance fields of M (respectively, Mk) have
/// initializers, they are executed in the scope of M (respectively, Mk) to
/// initialize the corresponding fields of C.
///
/// @description Checks that if mixins have initializers then they are executed
/// and initialize corresponding fields of the class.
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int _xa = 0;
  int get x => _xa;
}

mixin class M1 {
  int _xm1 = 1;
  int get x => _xm1;
}

mixin class M2 {
  int _xm2 = 2;
  int get x => _xm2;
}

class B1 extends A with M1 {
  test() {
    Expect.equals(1, super.x);
  }
}

class B2 extends A with M1, M2 {
  test() {
    Expect.equals(2, super.x);
  }
}

class B3 extends B1 with M2 {
  test() {
    Expect.equals(2, super.x);
  }
}

main() {
  B1 b1 = new B1();
  b1.test();

  B2 b2 = new B2();
  b2.test();

  B3 b3 = new B3();
  b3.test();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > initializers_t03', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion If any of the instance fields of M (respectively, Mk) have
/// initializers, they are executed in the scope of M (respectively, Mk) to
/// initialize the corresponding fields of C.
///
/// @description Checks that if mixins have initializers then they are executed
/// and initialize corresponding fields of the class C.
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int x1 = 1;

  testA() {
    Expect.equals(1, this.x1);
  }
}

mixin class M {
  int x2 = 2;

  testM() {
    Expect.equals(2, this.x2);
  }
}

class C extends A with M {
  test() {
    Expect.equals(1, x1);
    Expect.equals(2, x2);
    testM();
    testA();
  }
}


main() {
  C c = new C();
  c.test();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > initializers_t04', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion If any of the instance fields of M (respectively, Mk) have
/// initializers, they are executed in the scope of M (respectively, Mk) to
/// initialize the corresponding fields of C.
///
/// @description Checks that if mixins have initializers then they are executed
/// and initialize corresponding fields of the class. Test type aliases
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int _xa = 0;
  int get x => _xa;
}

mixin class M1 {
  int _xm1 = 1;
  int get x => _xm1;
}

mixin class M2 {
  int _xm2 = 2;
  int get x => _xm2;
}

typedef AAlias = A;
typedef M1Alias = M1;
typedef M2Alias = M2;

class B1 = AAlias with M1Alias;
class B2 = AAlias with M1Alias, M2Alias;
class B3 = B1 with M2Alias;

main() {
  B1 b1 = new B1();
  B2 b2 = new B2();
  B3 b3 = new B3();

  Expect.equals(1, b1.x);
  Expect.equals(2, b2.x);
  Expect.equals(2, b3.x);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > initializers_t05', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion If any of the instance fields of M (respectively, Mk) have
/// initializers, they are executed in the scope of M (respectively, Mk) to
/// initialize the corresponding fields of C.
///
/// @description Checks that if mixins have initializers then they are executed
/// and initialize corresponding fields of the class. Test type aliases
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int _xa = 0;
  int get x => _xa;
}

mixin class M1 {
  int _xm1 = 1;
  int get x => _xm1;
}

mixin class M2 {
  int _xm2 = 2;
  int get x => _xm2;
}

typedef AAlias = A;
typedef M1Alias = M1;
typedef M2Alias = M2;

class B1 extends AAlias with M1Alias {
  test() {
    Expect.equals(1, super.x);
  }
}

class B2 extends AAlias with M1Alias, M2Alias {
  test() {
    Expect.equals(2, super.x);
  }
}
class B3 extends B1 with M2Alias {
  test() {
    Expect.equals(2, super.x);
  }
}

main() {
  B1 b1 = new B1();
  b1.test();

  B2 b2 = new B2();
  b2.test();

  B3 b3 = new B3();
  b3.test();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > initializers_t06', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion If any of the instance fields of M (respectively, Mk) have
/// initializers, they are executed in the scope of M (respectively, Mk) to
/// initialize the corresponding fields of C.
///
/// @description Checks that if mixins have initializers then they are executed
/// and initialize corresponding fields of the class C.
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int x1 = 1;

  testA() {
    Expect.equals(1, this.x1);
  }
}

mixin class M {
  int x2 = 2;

  testM() {
    Expect.equals(2, this.x2);
  }
}

typedef AAlias = A;
typedef MAlias = M;

class C extends AAlias with MAlias {
  test() {
    Expect.equals(1, x1);
    Expect.equals(2, x2);
    testM();
    testA();
  }
}

main() {
  C c = new C();
  c.test();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > super_invocation_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a name
/// ...
/// It is a compile-time error if any of members contains a super-invocation of
/// a member m (for example super.foo, super + 2, or super[1] = 2), and S does
/// not have a concrete implementation of m which is a valid override of the
/// member m in the interface MS
///
/// @description Checks that it is not an error if `S` does not have a concrete
/// implementation of a member `m` but there are no super-invocation of `m`
/// @author sgrekhov22@gmail.com

abstract class S {
  bool get m;
  S operator +(int other);
  S operator [](int index);
}

mixin M1 on S {
  void foo() {}
}

mixin M2 on S {
  void foo() {}
}

mixin M3 on S {
  void foo() {}
}

abstract class C1 = S with M1;

abstract class C2 = S with M2;

abstract class C3 = S with M3;

main() {
  print(C1);
  print(C2);
  print(C3);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > super_invocation_t03', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a name
/// ...
/// It is a compile-time error if any of members contains a super-invocation of
/// a member m (for example super.foo, super + 2, or super[1] = 2), and S does
/// not have a concrete implementation of m which is a valid override of the
/// member m in the interface MS
///
/// @description Checks that it is not an error if any of members contains a
/// super-invocation of a member `m`, and `S` does have a concrete
/// implementation of `m` which is a valid override of the member `m` in the
/// interface `MS`
/// @author sgrekhov22@gmail.com

class A {
  Object m(num o) => o;
}

class S extends A {
  num m(Object n) => 42;
}

mixin M on A {
  void foo() {
    super.m(3.14);
  }
}

abstract class C1 = S with M;

abstract class C2 = A with M;

main() {
  print(C1);
  print(C2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > superinterfaces_t03', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// It is a compile-time error to apply M to S if S does not implement, directly
/// or indirectly, all of T1, . . . , Tn.
///
/// @description Checks that it is no error if `C` does implement directly all
/// of the direct superinterfaces of `M`
/// @author sgrekhov@unipro.ru

abstract class A {
  int get a;
}

abstract class B implements A {
  int get b;
}

abstract mixin class M implements B {
}

class S {
}

class C extends S with M {
  int get a => 0;
  int get b => 0;
}

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > superinterfaces_t04', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// It is a compile-time error to apply M to S if S does not implement, directly
/// or indirectly, all of T1, . . . , Tn.
///
/// @description Checks that it is no error if `C` does implement indirectly all
/// of the direct superinterfaces of `M`
/// @author sgrekhov@unipro.ru

abstract class A {
  int get a;
}

abstract class B implements A {
  int get b;
}

abstract mixin class M implements B {
}

class S {
  int get a => 0;
}

class C extends S with M {
  int get b => 0;
}

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > superinterfaces_t05', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// It is a compile-time error to apply M to S if S does not implement, directly
/// or indirectly, all of T1, . . . , Tn.
///
/// @description Checks that it is no error if `M` directly implements all of
/// its superinterfaces
/// @author sgrekhov@unipro.ru

class A {
  int get a => 0;
}

abstract class B implements A {
  int get b;
}

mixin class M implements B {
  int get a => -1;
  int get b => 1;
}

class S {
}

class C extends S with M {
}

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > superinterfaces_t07', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// It is a compile-time error to apply M to S if S does not implement, directly
/// or indirectly, all of T1, . . . , Tn.
///
/// @description Checks that it is no error if `M` implements all of its
/// superinterfaces
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int get a => 1;
}

class B extends A {
  int get b => 2;
}

mixin M on B {
  int get c => 3;
}

class S extends B {
  int get a => -1;
}

class C extends S with M {
  int get b => -2;
}

main() {
  C c = new C();
  Expect.equals(-1, c.a);
  Expect.equals(-2, c.b);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > superinterfaces_t09', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// It is a compile-time error to apply M to S if S does not implement, directly
/// or indirectly, all of T1, . . . , Tn.
///
/// @description Checks that it is no compile error if `M` has a superinterfaces
/// and `C` does implement them. Test `class C = S with M;` syntax
/// @author sgrekhov@unipro.ru

abstract class A {
  int get a;
}

abstract class B {
  int get b;
}

abstract mixin class M implements A, B {
}

class S {
  int get a => 0;
  int get b => 1;
}

class C = S with M;

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > superinterfaces_t11', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// It is a compile-time error to apply M to S if S does not implement, directly
/// or indirectly, all of T1, . . . , Tn.
///
/// @description Checks that it is no error if `C` does implement directly all
/// of the direct superinterfaces of `M`. Test type aliases
/// @author sgrekhov@unipro.ru

abstract class A {
  int get a;
}

abstract class B implements A {
  int get b;
}

abstract mixin class M implements B {
}

class S {
}

typedef MAlias = M;

class C extends S with MAlias {
  int get a => 0;
  int get b => 0;
}

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > superinterfaces_t12', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// It is a compile-time error to apply M to S if S does not implement, directly
/// or indirectly, all of T1, . . . , Tn.
///
/// @description Checks that it is no error if `M` directly implements all of
/// its superinterfaces
/// @author sgrekhov@unipro.ru

class A {
  int get a => 0;
}

abstract class B implements A {
  int get b;
}

typedef BAlias = B;

mixin class M implements BAlias {
  int get a => -1;
  int get b => 1;
}

class S {
}

class C extends S with M {
}

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > superinterfaces_t14', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// It is a compile-time error to apply M to S if S does not implement, directly
/// or indirectly, all of T1, . . . , Tn.
///
/// @description Checks that it is no compile error if `M` has a superinterfaces
/// and `C` does implement all of them. Test `class C = S with M;` syntax. Test
/// type alias
/// @author sgrekhov@unipro.ru

abstract class A {
  int get a;
}

abstract class B {
  int get b;
}

abstract mixin class M implements A, B {
}

typedef MAlias = M;

class S {
  int get a => 0;
  int get b => 1;
}

class C = S with MAlias;

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > syntax_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// ...
/// The mixin application of M to S with name N introduces a new class, C, with
/// name N, superclass S, implemented interface I1, . . . , Ik and members as
/// instance members.
///
/// @description Checks that an instance class has the same instance getters as
/// its mixins
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int get ga => 0;
}

mixin class M1 {
  int get gm1 => 1;
}

mixin class M2 {
  int get gm2 => 2;
}

class B1 = A with M1;
class B2 = A with M1, M2;
class B3 = B1 with M2;

main() {
  B1 b1 = new B1();
  B2 b2 = new B2();
  B3 b3 = new B3();

  Expect.equals(0, b1.ga);
  Expect.equals(0, b2.ga);
  Expect.equals(0, b3.ga);

  Expect.equals(1, b1.gm1);
  Expect.equals(1, b2.gm1);
  Expect.equals(1, b3.gm1);

  Expect.equals(2, b2.gm2);
  Expect.equals(2, b3.gm2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > syntax_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// ...
/// The mixin application of M to S with name N introduces a new class, C, with
/// name N, superclass S, implemented interface I1, . . . , Ik and members as
/// instance members.
///
/// @description Checks that instance class has the same instance setters as
/// its mixins
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int x = 0;
  set sa(int val) {x = val;}
  int get ga => x;
}

mixin class M1 {
  int x = 1;
  set s1(int val) {x = val;}
  int get g1 => x;
}

mixin class M2 {
  int x = 2;
  set s2(int val) {x = val;}
  int get g2 => x;
}

class B1 = A with M1;
class B2 = A with M1, M2;
class B3 = B1 with M2;

main() {
  B1 b1 = new B1();
  B2 b2 = new B2();
  B3 b3 = new B3();

  b1.sa = 1;
  b2.sa = 2;
  b3.sa = 3;

  Expect.equals(1, b1.ga);
  Expect.equals(2, b2.ga);
  Expect.equals(3, b3.ga);

  b1.s1 = 11;
  b2.s1 = 12;
  b3.s1 = 13;

  Expect.equals(11, b1.g1);
  Expect.equals(12, b2.g1);
  Expect.equals(13, b3.g1);

  b2.s2 = 22;
  b3.s2 = 23;

  Expect.equals(22, b2.g2);
  Expect.equals(23, b3.g2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > syntax_t03', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// ...
/// The mixin application of M to S with name N introduces a new class, C, with
/// name N, superclass S, implemented interface I1, . . . , Ik and members as
/// instance members.
///
/// @description Checks that an instance class has the same instance methods as
/// its mixins
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int x = 0;
  void ma(int val) {x = val;}
  int get ga => x;
}

mixin class M1 {
  int x = 1;
  void m1(int val) {x = val;}
  int get g1 => x;
}

mixin class M2 {
  int x = 2;
  void m2(int val) {x = val;}
  int get g2 => x;
}

class B1 = A with M1;
class B2 = A with M1, M2;
class B3 = B1 with M2;

main() {
  B1 b1 = new B1();
  B2 b2 = new B2();
  B3 b3 = new B3();

  b1.ma(1);
  b2.ma(2);
  b3.ma(3);

  Expect.equals(1, b1.ga);
  Expect.equals(2, b2.ga);
  Expect.equals(3, b3.ga);

  b1.m1(11);
  b2.m1(12);
  b3.m1(13);

  Expect.equals(11, b1.g1);
  Expect.equals(12, b2.g1);
  Expect.equals(13, b3.g1);

  b2.m2(22);
  b3.m2(23);

  Expect.equals(22, b2.g2);
  Expect.equals(23, b3.g2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > syntax_t04', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// ...
/// The mixin application of M to S with name N introduces a new class, C, with
/// name N, superclass S, implemented interface I1, . . . , Ik and members as
/// instance members.
///
/// @description Checks that an instance class has the same instance variables
/// as its mixins
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int x = 0;
  int get ga => x;
}

mixin class M1 {
  int x = 1;
  int y = 1;
  int get g1x => x;
  int get g1y => y;
}

mixin class M2 {
  int x = 2;
  int z = 2;
  int get g2x => x;
  int get g2z => z;
}

class B1 = A with M1;
class B2 = A with M1, M2;
class B3 = B1 with M2;

main() {
  B1 b1 = new B1();
  B2 b2 = new B2();
  B3 b3 = new B3();

  b1.x = 1;
  b2.x = 2;
  b3.x = 3;

  Expect.equals(1, b1.x);
  Expect.equals(2, b2.x);
  Expect.equals(3, b3.x);
  Expect.equals(1, b1.ga);
  Expect.equals(2, b2.ga);
  Expect.equals(3, b3.ga);
  Expect.equals(1, b1.g1x);
  Expect.equals(2, b2.g1x);
  Expect.equals(3, b3.g1x);

  b1.y = 11;
  b2.y = 12;
  b3.y = 13;

  Expect.equals(11, b1.y);
  Expect.equals(12, b2.y);
  Expect.equals(13, b3.y);
  Expect.equals(11, b1.g1y);
  Expect.equals(12, b2.g1y);
  Expect.equals(13, b3.g1y);

  b2.z = 22;
  b3.z = 23;

  Expect.equals(22, b2.z);
  Expect.equals(23, b3.z);
  Expect.equals(22, b2.g2z);
  Expect.equals(23, b3.g2z);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > syntax_t05', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// ...
/// The mixin application of M to S with name N introduces a new class, C, with
/// name N, superclass S, implemented interface I1, . . . , Ik and members as
/// instance members.
///
/// @description Checks that a mixin application of the form `S with M;` defines
/// a class `C` with superclass `S`. So if `M` and `S` have methods with the
/// same names then the mixin method overrides the one from `S`. Test
/// `class C = S with M` syntax
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class S {
  String get m => "S";
}

mixin class M {
  String get m => "M";
}

class C = S with M;

main() {
  C c = new C();
  Expect.equals("M", c.m);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > syntax_t06', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// ...
/// The mixin application of M to S with name N introduces a new class, C, with
/// name N, superclass S, implemented interface I1, . . . , Ik and members as
/// instance members.
///
/// @description Checks that a mixin application of the form `S with M;` defines
/// a class `C` with superclass `S`. So if `M` and `S` have getters with the
/// same names then the mixin getter overrides the one from S
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class S {
  String get m => "S";
}

mixin class M {
  String get m => "M";
}

class C extends S with M {
  test() {
    Expect.equals("M", super.m);
  }
}

main() {
  C c = new C();
  c.test();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > syntax_t07', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// ...
/// The mixin application of M to S with name N introduces a new class, C, with
/// name N, superclass S, implemented interface I1, . . . , Ik and members as
/// instance members.
///
/// @description Test that instance of class `(S with M)` is `S`
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class S {
}

mixin class M {
}

class C1 = S with M;

class C2 extends S with M {
}

main() {
  C1 c1 = new C1();
  Expect.isTrue(c1 is S);
  Expect.runtimeIsType<S>(c1);

  C2 c2 = new C2();
  Expect.isTrue(c2 is S);
  Expect.runtimeIsType<S>(c2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > syntax_t08', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// ...
/// The mixin application of M to S with name N introduces a new class, C, with
/// name N, superclass S, implemented interface I1, . . . , Ik and members as
/// instance members.
///
/// @description Checks that a mixin application of the form `S with M1,...,Mk;`
/// defines a class `C` with superclass `S with M1,...Mk-1`. So if `Mi` have
/// methods with the same names then `Mk` method overrides the ones from `Mi`
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class S {
  String get m => "S";
}

mixin class M1 {
  String get m => "M1";
}

mixin class M2 {
  String get m => "M2";
}

mixin class M3 {
  String get m => "M3";
}

class C1 = S with M1;
class C2 = S with M1, M2;
class C3 = S with M1, M2, M3;

main() {
  C1 c1 = new C1();
  Expect.equals("M1", c1.m);

  C2 c2 = new C2();
  Expect.equals("M2", c2.m);

  C3 c3 = new C3();
  Expect.equals("M3", c3.m);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > syntax_t09', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// ...
/// The mixin application of M to S with name N introduces a new class, C, with
/// name N, superclass S, implemented interface I1, . . . , Ik and members as
/// instance members.
///
/// @description Checks that a mixin application of the form `S with M1,...,Mk;`
/// defines a class `C` with superclass `S with M1,...Mk-1`. So if `Mi` have
/// methods with the same names then `Mk` method overrides the ones from `Mi`
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class S {
  String get m => "S";
}

mixin class M1 {
  String get m => "M1";
}

mixin class M2 {
  String get m => "M2";
}

mixin class M3 {
  String get m => "M3";
}

class C1 extends S with M1 {
  test() {
    Expect.equals("M1", super.m);
  }
}

class C2 extends S with M1, M2 {
  test() {
    Expect.equals("M2", super.m);
  }
}

class C3 extends S with M1, M2, M3 {
  test() {
    Expect.equals("M3", super.m);
  }
}

main() {
  C1 c1 = new C1();
  c1.test();

  C2 c2 = new C2();
  c2.test();

  C3 c3 = new C3();
  c3.test();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > syntax_t10', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// ...
/// The mixin application of M to S with name N introduces a new class, C, with
/// name N, superclass S, implemented interface I1, . . . , Ik and members as
/// instance members.
///
/// @description Test that instance of class `(S with M1, M2)` is `S`
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class S {
}

mixin class M1 {
}

mixin class M2 {
}

class C extends S with M1, M2 {
}

main() {
  C c = new C();
  Expect.isTrue(c is S);
  Expect.runtimeIsType<S>(c);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > syntax_t11', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// ...
/// The mixin application of M to S with name N introduces a new class, C, with
/// name N, superclass S, implemented interface I1, . . . , Ik and members as
/// instance members.
///
/// @description Checks that instance class has the same instance getters as
/// its mixins. Test type aliases
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int get ga => 0;
}

mixin class M1 {
  int get gm1 => 1;
}

mixin class M2 {
  int get gm2 => 2;
}

typedef MAlias1 = M1;
typedef MAlias2 = M2;

class B1 = A with MAlias1;
class B2 = A with MAlias1, MAlias2;
class B3 = B1 with MAlias2;

main() {
  B1 b1 = new B1();
  B2 b2 = new B2();
  B3 b3 = new B3();

  Expect.equals(0, b1.ga);
  Expect.equals(0, b2.ga);
  Expect.equals(0, b3.ga);

  Expect.equals(1, b1.gm1);
  Expect.equals(1, b2.gm1);
  Expect.equals(1, b3.gm1);

  Expect.equals(2, b2.gm2);
  Expect.equals(2, b3.gm2);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Application > syntax_t12', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2023, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let S be a class, M be a mixin with required superinterfaces
/// T1, . . . , Tn, combined superinterface MS, implemented interfaces
/// I1, . . . , Ik and members as mixin member declarations, and let N be a
/// name.
/// ...
/// The mixin application of M to S with name N introduces a new class, C,
/// with name N, superclass S, implemented interface I1, . . . , Ik and members
/// as instance members.
///
/// @description Checks that it is not an error if a class mixes in and
/// implements the same mixin class
/// @author sgrekhov22@gmail.com

mixin class M {
  num foo(int i) => i;
}

class C1 with M implements M {}
class C2 = Object with M implements M;

main() {
  C1();
  C2();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > abstract_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of
/// L, bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
/// Iff the class declaration is prefixed by the built-in identifier abstract,
/// the class being defined is made an abstract class
///
/// @description Checks that class `C` is not an abstract (can be instantiated
/// by `new`) if mixin application is not abstract and there is no `abstract`
/// identifier before class `C` declaration
/// @author sgrekhov@unipro.ru

class S {
}

mixin class M {
}

class C = S with M;

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > abstract_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of
/// L, bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
/// Iff the class declaration is prefixed by the built-in identifier abstract,
/// the class being defined is made an abstract class
///
/// @description Checks that class `C` is not an abstract (can be instantiated
/// by `new`) if there is no `abstract` identifier but `M` in `S with M` is an
/// abstract
/// @author sgrekhov@unipro.ru

abstract mixin class M {
}

class S {
}

class C = S with M;

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > abstract_t03', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of
/// L, bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
/// Iff the class declaration is prefixed by the built-in identifier abstract,
/// the class being defined is made an abstract class
///
/// @description Checks that class `C` is not an abstract (can be instantiated
/// by `new`) if there is no `abstract` identifier but `S` in `S with M` is
/// abstract
/// @author sgrekhov@unipro.ru

mixin class M {
}

abstract class S {
}

class C = S with M;

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > abstract_t04', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of
/// L, bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
/// Iff the class declaration is prefixed by the built-in identifier abstract,
/// the class being defined is made an abstract class
///
/// @description Checks that class `C` is not an abstract (can be instantiated
/// by `new`) if there is no `abstract` identifier but all classes in a mixin
/// application are abstract
/// @author sgrekhov@unipro.ru

abstract mixin class M {
}

abstract class S {
}

class C = S with M;

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > abstract_t09', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of
/// L, bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
/// Iff the class declaration is prefixed by the built-in identifier abstract,
/// the class being defined is made an abstract class
///
/// @description Checks that class `C` is not an abstract (can be instantiated
/// by `new`) if mixin application is not abstract and there is no abstract
/// identifier. Test type aliases
/// @author sgrekhov@unipro.ru

class S {
}

mixin class M {
}

typedef SAlias = S;
typedef MAlias = M;

class C = SAlias with MAlias;

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > abstract_t10', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of
/// L, bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
/// Iff the class declaration is prefixed by the built-in identifier abstract,
/// the class being defined is made an abstract class
///
/// @description Checks that class `C` is not an abstract (can be instantiated
/// by `new`) if there are no `abstract` identifier but `M` in `S with M` is an
/// abstract. Test type aliases
/// @author sgrekhov@unipro.ru

abstract mixin class M {
}

class S {
}

typedef SAlias = S;
typedef MAlias = M;

class C = SAlias with MAlias;

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > abstract_t11', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of
/// L, bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
/// Iff the class declaration is prefixed by the built-in identifier abstract,
/// the class being defined is made an abstract class
///
/// @description Checks that class `C` is not an abstract (can be instantiated
/// by `new`) if there is no `abstract` identifier but `S` in `S with M` is an
/// abstract. Test type aliases
/// @author sgrekhov@unipro.ru

mixin class M {
}

abstract class S {
}

typedef SAlias = S;
typedef MAlias = M;

class C = SAlias with MAlias;

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > abstract_t12', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of
/// L, bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
/// Iff the class declaration is prefixed by the built-in identifier abstract,
/// the class being defined is made an abstract class
///
/// @description Checks that class `C` is not an abstract (can be instantiated
/// by `new`) if there is no `abstract` identifier and all classes in a  mixin
/// application are abstract. Test type aliases
/// @author sgrekhov@unipro.ru

abstract mixin class M {
}

abstract class S {
}

typedef SAlias = S;
typedef MAlias = M;

class C = SAlias with MAlias;

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > interfaces_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of L,
/// bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
///
/// @description Checks that if the mixin application declares interfaces then
/// the resulting class implements those interfaces
/// @author sgrekhov@unipro.ru

abstract class I {
  num get g;
}

class A {
}

mixin class M {
}

abstract class B = A with M implements I;

class C implements B {
  num get g => 0;
}

main() {
  C c = new C();
  c.g;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > interfaces_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of L,
/// bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
///
/// @description Checks that if the mixin application declares interfaces then
/// the resulting class implements those interfaces
/// @author sgrekhov@unipro.ru

abstract class I {
  num get g;
}

class S {
  num get g => 0;
}

mixin class M {
}

class C = S with M implements I;

main() {
  C c = new C();
  c.g;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > interfaces_t03', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of L,
/// bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
///
/// @description Checks that if the mixin application declares interfaces then
/// the resulting class implements those interfaces. Interface implementation is
/// declared in a mixin
/// @author sgrekhov@unipro.ru

abstract class I {
  num get g;
}

class S {
}

mixin class M {
  num get g => 0;
}

class C = S with M implements I;

main() {
  C c = new C();
  c.g;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > interfaces_t04', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of L,
/// bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
///
/// @description Checks that if the mixin application declares interfaces then
/// the resulting class implements those interfaces. Interface implementation is
/// declared partially in a mixin, partially in a superclass
/// @author sgrekhov@unipro.ru

abstract class I {
  num get g1;
  String get g2;
}

class S {
  num get g1 => 0;
}

mixin class M {
  String get g2 => "M";
}

class C = S with M implements I;

main() {
  C c = new C();
  c.g1;
  c.g2;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > interfaces_t06', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of L,
/// bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
///
/// @description Checks that if the mixin application declares interfaces then
/// the resulting class implements those interfaces. Test type aliases
/// @author sgrekhov@unipro.ru

abstract class I {
  num get g;
}

class A {
}

mixin class M {
}

typedef IAlias = I;
typedef AAlias = A;
typedef MAlias = M;

abstract class B = AAlias with MAlias implements IAlias;

class C implements B {
  num get g => 0;
}

main() {
  C c = new C();
  c.g;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > interfaces_t07', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion Let D be a mixin application class declaration of the form
/// abstract? class N = S with M1, ..., Mn implements I1, ..., Ik;
/// ...
/// The effect of D in library L is to introduce the name N into the scope of L,
/// bound to the class defined by the clause S with M1, ..., Mn with name N,
/// as described below. If k > 0 then the class also implements I1, . . . , Ik.
///
/// @description Checks that if the mixin application declares interfaces then
/// the resulting class implements those interfaces. Interface implementation is
/// declared partially in a mixin, partially in a superclass. Test type alias
/// @author sgrekhov@unipro.ru

abstract class I {
  num get g1;
  String get g2;
}

class S {
  num get g1 => 0;
}

mixin class M {
  String get g2 => "M";
}

typedef IAlias = I;
typedef SAlias = S;
typedef MAlias = M;

class C = SAlias with MAlias implements IAlias;

main() {
  C c = new C();
  c.g1;
  c.g2;
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > order_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2011, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion If there is more than one mixin (n > 1), then let X be the class
/// defined by S with M1, ..., Mn−1 with name F, where F is a fresh name, and
/// make X abstract. Then S with M1, ..., Mn defines the class yielded by the
/// mixin application of the mixin of Mn to the class X with name N.
///
/// @description Checks that mixin composition applies mixins in the right
/// order
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int m = 0;
}

mixin class M1 {
  int m = 1;
}

mixin class M2 {
  int m = 2;
}

mixin class M3 {
  int m = 3;
}

class B1 = A with M1, M2;
class B2 = A with M1, M2, M3;
class B3 = B1 with M3, M2, M1;

main() {
  B1 b1 = new B1();
  B2 b2 = new B2();
  B3 b3 = new B3();

  Expect.equals(2, b1.m);
  Expect.equals(3, b2.m);
  Expect.equals(1, b3.m);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > order_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2019, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion If there is more than one mixin (n > 1), then let X be the class
/// defined by S with M1, ..., Mn−1 with name F, where F is a fresh name, and
/// make X abstract. Then S with M1, ..., Mn defines the class yielded by the
/// mixin application of the mixin of Mn to the class X with name N.
///
/// @description Checks that mixin composition applies mixins in the right
/// order. Test type aliases
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  int m = 0;
}

mixin class M1 {
  int m = 1;
}

mixin class M2 {
  int m = 2;
}

mixin class M3 {
  int m = 3;
}

typedef AAlias = A;
typedef MAlias1 = M1;
typedef MAlias2 = M2;
typedef MAlias3 = M3;

class B1 = AAlias with MAlias1, MAlias2;
class B2 = AAlias with MAlias1, MAlias2, MAlias3;
class B3 = B1 with MAlias3, MAlias2, MAlias1;

main() {
  B1 b1 = new B1();
  B2 b2 = new B2();
  B3 b3 = new B3();

  Expect.equals(2, b1.m);
  Expect.equals(3, b2.m);
  Expect.equals(1, b3.m);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > syntax_A03_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion
/// ⟨mixinApplicationClass⟩ ::=
///     ⟨identifier⟩ ⟨typeParameters⟩? ‘=’ ⟨mixinApplication⟩ ‘;’
/// ⟨mixinApplication⟩ ::= ⟨typeNotVoid⟩ ⟨mixins⟩ ⟨interfaces⟩?
/// ...
/// A clause of the form S with M1, ..., Mn with name N defines a
/// class as follows:
/// If there is only one mixin (n = 1), then S with M1 defines the class yielded
/// by the mixin application of the mixin of M1 to the class denoted by S with
/// name N.
/// If there is more than one mixin (n > 1), then let X be the class defined by
/// S with M1, ..., Mn−1 with name F, where F is a fresh name, and make X
/// abstract. Then S with M1, ..., Mn defines the class yielded by the mixin
/// application of the mixin of Mn to the class X with name N.
///
/// @description Test that mixin can be derived from the superclass
/// @author sgrekhov@unipro.ru
/// @issue 25765
/// @issue 42256
/// @issue 52859

mixin class S {}

class C = S with S;

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > syntax_A03_t03', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion
/// ⟨mixinApplicationClass⟩ ::=
///     ⟨identifier⟩ ⟨typeParameters⟩? ‘=’ ⟨mixinApplication⟩ ‘;’
/// ⟨mixinApplication⟩ ::= ⟨typeNotVoid⟩ ⟨mixins⟩ ⟨interfaces⟩?
/// ...
/// A clause of the form S with M1, ..., Mn with name N defines a
/// class as follows:
/// If there is only one mixin (n = 1), then S with M1 defines the class yielded
/// by the mixin application of the mixin of M1 to the class denoted by S with
/// name N.
/// If there is more than one mixin (n > 1), then let X be the class defined by
/// S with M1, ..., Mn−1 with name F, where F is a fresh name, and make X
/// abstract. Then S with M1, ..., Mn defines the class yielded by the mixin
/// application of the mixin of Mn to the class X with name N.
///
/// @description Test that mixin can be mixed into several times
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class S {
  var s;
}

mixin class M {
  int get m => 1;
}

class C extends S with M, M, M {
  var c;
}

main() {
  C c = new C();
  Expect.equals(1, c.m);
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > syntax_A04_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion
/// ⟨mixinApplicationClass⟩ ::=
///     ⟨identifier⟩ ⟨typeParameters⟩? ‘=’ ⟨mixinApplication⟩ ‘;’
/// ⟨mixinApplication⟩ ::= ⟨typeNotVoid⟩ ⟨mixins⟩ ⟨interfaces⟩?
/// ...
/// A clause of the form S with M1, ..., Mn with name N defines a
/// class as follows:
/// If there is only one mixin (n = 1), then S with M1 defines the class yielded
/// by the mixin application of the mixin of M1 to the class denoted by S with
/// name N.
/// If there is more than one mixin (n > 1), then let X be the class defined by
/// S with M1, ..., Mn−1 with name F, where F is a fresh name, and make X
/// abstract. Then S with M1, ..., Mn defines the class yielded by the mixin
/// application of the mixin of Mn to the class X with name N.
///
/// @description Test that mixin may have type parameters
/// @author sgrekhov@unipro.ru

// [co19_adapter] removed Utils import

class S<T> {
  T? s;
}

mixin class M<T1, T2> {
  T1? m1;
  T2? m2;
}

class C<T> extends S<T> with M<T, String> {
  T? c;
}

main() {
  C<int> c = new C();
  c.m1.expectStaticType<Exactly<int?>>();
  c.m2.expectStaticType<Exactly<String?>>();
  c.s.expectStaticType<Exactly<int?>>();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > syntax_A04_t02', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion
/// ⟨mixinApplicationClass⟩ ::=
///     ⟨identifier⟩ ⟨typeParameters⟩? ‘=’ ⟨mixinApplication⟩ ‘;’
/// ⟨mixinApplication⟩ ::= ⟨typeNotVoid⟩ ⟨mixins⟩ ⟨interfaces⟩?
/// ...
/// A clause of the form S with M1, ..., Mn with name N defines a
/// class as follows:
/// If there is only one mixin (n = 1), then S with M1 defines the class yielded
/// by the mixin application of the mixin of M1 to the class denoted by S with
/// name N.
/// If there is more than one mixin (n > 1), then let X be the class defined by
/// S with M1, ..., Mn−1 with name F, where F is a fresh name, and make X
/// abstract. Then S with M1, ..., Mn defines the class yielded by the mixin
/// application of the mixin of Mn to the class X with name N.
///
/// @description Test that mixin may have type parameters. Test an instantiation
/// to bounds
/// @author sgrekhov@unipro.ru

// [co19_adapter] removed Utils import

class S<T> {
  T? s;
}

mixin class M<T1 extends num, T2 extends String> {
  T1? m1;
  T2? m2;
}

class C extends S with M {}

main() {
  C c = new C();
  c.m1.expectStaticType<Exactly<num?>>();
  c.m2.expectStaticType<Exactly<String?>>();
  if (1 > 2) {
    c.s.whatever; // s is dynamic
  }
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Classes > syntax_A06_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion
/// ⟨mixinApplicationClass⟩ ::=
///     ⟨identifier⟩ ⟨typeParameters⟩? ‘=’ ⟨mixinApplication⟩ ‘;’
/// ⟨mixinApplication⟩ ::= ⟨typeNotVoid⟩ ⟨mixins⟩ ⟨interfaces⟩?
/// ...
/// A clause of the form S with M1, ..., Mn with name N defines a
/// class as follows:
/// If there is only one mixin (n = 1), then S with M1 defines the class yielded
/// by the mixin application of the mixin of M1 to the class denoted by S with
/// name N.
/// If there is more than one mixin (n > 1), then let X be the class defined by
/// S with M1, ..., Mn−1 with name F, where F is a fresh name, and make X
/// abstract. Then S with M1, ..., Mn defines the class yielded by the mixin
/// application of the mixin of Mn to the class X with name N.
///
/// @description Checks that it is not an error if mixin references members of
/// 'Object' via 'super'
/// @issue 26409
/// @issue 42255
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
}

mixin class M {
  String m() {
    return super.toString();
  }
}

class C = A with M;

main() {
  C c = new C();
  Expect.isNotNull(c.m());
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('Mixin_Declaration > no_on_clause_t01', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2016, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion A mixin declaration introduces a mixin and provides a scope for
/// static member declarations.
/// ⟨mixinDeclaration⟩ ::= mixin ⟨typeIdentifier⟩ ⟨typeParameters⟩?
///     (on ⟨typeNotVoidList⟩)? ⟨interfaces⟩?
///     ‘{’ (⟨metadata⟩ ⟨classMemberDeclaration⟩)* ‘}’
/// ...
/// A mixin declaration with no on clause is equivalent to one with the clause
/// on Object.
///
/// @description Checks that it is no compile error to declare mixin on a class
/// which has a superclass other than `Object`.
/// @issue 26409
/// @author sgrekhov@unipro.ru

import 'package:co19_expect/expect.dart';

class A {
  String a() => "A";
}

class S extends A {
}

mixin M on A {
  String m() {
    return super.a();
  }
}

class C = S with M;

main() {
  C c = new C();
  Expect.equals("A", c.m());
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('declaring_constructor_t05', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion It is a compile-time error to derive a mixin from a class that
/// declares a generative constructor, or from a class that has a superclass
/// other than Object.
///
/// @description Checks that it is no compile-time error if a derived mixin
/// explicitly declares a factory constructor.
/// @issue 24767
/// @author sgrekhov@unipro.ru

class A {
}

mixin class M {
  factory M() => new B();
}

class B implements M {}

class C extends A with M {
}

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
    test('declaring_constructor_t06', () {
      final plugin = Co19ExpectPlugin();
      final compiler = Compiler();
      compiler.addPlugin(plugin);
      final runtime = compiler.compileWriteAndLoad({
        'co19_test': {
          'main.dart': '''
// Copyright (c) 2015, the Dart project authors.  Please see the AUTHORS file
// for details. All rights reserved. Use of this source code is governed by a
// BSD-style license that can be found in the LICENSE file.

/// @assertion It is a compile-time error to derive a mixin from a class that
/// declares a generative constructor, or from a class that has a superclass
/// other than Object.
///
/// @description Checks that it is no compile-time error if a derived mixin
/// explicitly declares a factory constructor.
/// @issue 24767
/// @author sgrekhov@unipro.ru

class A {
}

mixin class M {
  factory M() => new B();
}

class B implements M {}

class C = A with M;

main() {
  new C();
}

'''
        }
      });
      plugin.configureForRuntime(runtime);
      runtime.executeLib('package:co19_test/main.dart', 'main');
    });
  });
}
